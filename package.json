{
  "name": "leetcode-clone",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend-vue\"",
    "start:backend": "cd backend && npm install && npm run dev",
    "start:frontend-vue": "cd frontend-vue && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
